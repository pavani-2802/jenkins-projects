pipeline {

   agent { 
       docker { 
           image 'maven:3.8.1-openjdk-17' 
           // You still need to add this argument for Docker access within the container
           // args '-v /var/run/docker.sock:/var/run/docker.sock'
       } 
   }
   
   stages {
    
    // --- STAGE 1: Checkout (Assuming Jenkins handles the clone automatically) ---
    stage('Checkout') {
      steps {
        sh 'echo passed'
        //git branch: 'main', url: 'https://github.com/pavani-2802/jenkins-projects.git'
      }
    }
    
    // --- STAGE 2: Build and Test ---
    stage('Build and Test') {
      steps {
        sh 'ls -ltra'
        sh 'cd java-maven-sonar-argocd-helm-k8s/spring-boot-app && mvn clean package'
      }
    }
    
    // --- STAGE 3: Static Code Analysis ---
     stage('Static Code Analysis') {
      environment {
        SONAR_URL = "http://192.168.41.130:9000"
      }
      steps {
        withCredentials([string(credentialsId: 'sonarqube', variable: 'SONAR_AUTH_TOKEN')]) {
          sh 'cd java-maven-sonar-argocd-helm-k8s/spring-boot-app && mvn sonar:sonar -Dsonar.login=$SONAR_AUTH_TOKEN -Dsonar.host.url=${SONAR_URL}'
        }
      }
    }
    
    // --- STAGE 4: Build and Push Docker Image (Combined and Corrected) ---
     stage('Build and Push Docker Image') {
        steps {
            script {
                // Ensure DOCKER_IMAGE is defined somewhere (e.g. in an 'environment' block at the top level or within the script)
                // For example: def DOCKER_IMAGE = "pavanif5/jenkins-projects:${env.BUILD_NUMBER}"

                // 1. Build the image
                sh 'cd java-maven-sonar-argocd-helm-k8s/spring-boot-app && docker build -t ${DOCKER_IMAGE} .'
                
                // 2. Push the image using correct registry credentials
                def dockerImage = docker.image("${DOCKER_IMAGE}")
                // Use the correct credentials ID you created earlier (e.g., 'docker-hub-login')
                docker.withRegistry('https://index.docker.io/v1/', "docker-hub-login") { 
                    dockerImage.push()
                }
            }
        }
    }
    
    // --- STAGE 5: Update Deployment File ---
    stage('Update Deployment File') {
        environment {
            GIT_REPO_NAME = "jenkins-projects"
            GIT_USER_NAME = "pavani-2802"
        }
        steps {
            withCredentials([string(credentialsId: 'github', variable: 'GITHUB_TOKEN')]) {
                sh '''
                    git config user.email "pavanikethari.28@gmail.com"
                    git config user.name "PavaniKethari28"
                    BUILD_NUMBER=${BUILD_NUMBER}
                    sed -i "s/replaceImageTag/${BUILD_NUMBER}/g" java-maven-sonar-argocd-helm-k8s/spring-boot-app-manifests/deployment.yml
                    git add java-maven-sonar-argocd-helm-k8s/spring-boot-app-manifests/deployment.yml
                    git commit -m "Update deployment image to version ${BUILD_NUMBER}"
                    git push https://${GITHUB_TOKEN}@github.com/${GIT_USER_NAME}/${GIT_REPO_NAME} HEAD:main
                '''
            }
        }
    }
  } 
} 
