 pipeline {
  agent {
    docker {
      image 'my-maven-docker:latest'
      // Mount Docker socket and explicitly use the 'docker' group for permissions
      args '--user 0:0 -v /var/run/docker.sock:/var/run/docker.sock' 
    }
  }
  
  environment {
      DOCKER_IMAGE = "pavanif5/jenkins-projects:${BUILD_NUMBER}"
      // Define all credential IDs here for clarity
      SONAR_CRED_ID = 'sonarqube'
      DOCKER_CRED_ID = 'docker-cred'
      GITHUB_CRED_ID = 'github'
      // Set the required API version globally
      DOCKER_API_VERSION = '1.41' 
  }

  stages {
    stage('Checkout') {
      steps {
        // Use the built-in Jenkins step for robust SCM checkout
        // Assumes the repo is the one the pipeline job is configured for
        checkout scm
      }
    }
    
    stage('Build and Test') {
      steps {
        sh 'echo "Current directory contents:"'
        sh 'ls -ltra'
        // Run commands relative to the workspace root
        sh 'mvn clean package' 
      }
    }
    
    stage('Static Code Analysis') {
      environment {
        SONAR_URL = "http://192.168.41.130:9000"
      }
      steps {
        withCredentials([string(credentialsId: env.SONAR_CRED_ID, variable: 'SONAR_AUTH_TOKEN')]) {
          // Pass directory as a parameter to the command instead of 'cd'
          sh 'mvn sonar:sonar -Dsonar.login=$SONAR_AUTH_TOKEN -Dsonar.host.url=${SONAR_URL} -f java-maven-sonar-argocd-helm-k8s/spring-boot-app/pom.xml'
        }
      }
    }
    
    stage('Build and Push Docker Image') {
      steps {
        script {
            // Build the image using the relative Dockerfile location
            sh 'docker build -t ${DOCKER_IMAGE} -f java-maven-sonar-argocd-helm-k8s/spring-boot-app/Dockerfile java-maven-sonar-argocd-helm-k8s/spring-boot-app/'
            
            // Use docker.withRegistry with the defined credentials ID
            docker.withRegistry('index.docker.io', env.DOCKER_CRED_ID) {
                // Push the specific image tag defined globally
                docker.image("${DOCKER_IMAGE}").push()
            }
        }
      }
    }
    
    stage('Update Deployment File') {
        environment {
            GIT_REPO_NAME = "jenkins-Projects" // Verify this capitalization matches GitHub
            GIT_USER_NAME = "pavani-2802"
            GIT_USER_EMAIL = "pavanikethari.28@gmail.com"
        }

        steps {
            withCredentials([string(credentialsId: env.GITHUB_CRED_ID, variable: 'GITHUB_TOKEN')]) {
                sh '''
                    # Configure git within the workspace
                    git config user.email "${GIT_USER_EMAIL}"
                    git config user.name "PavaniKethari28"
                    
                    # Update YAML file in place
                    DEPLOY_FILE="java-maven-sonar-argocd-helm-k8s/spring-boot-app-manifests/deployment.yml"
                    sed -i "s/replaceImageTag/${BUILD_NUMBER}/g" $DEPLOY_FILE
                    
                    # Commit and Push
                    git add $DEPLOY_FILE
                    git commit -m "Update deployment image to version ${BUILD_NUMBER}"
                    # Use HTTPS URL with token authentication
                    git push https://${GITHUB_TOKEN}@github.com{GIT_USER_NAME}/${GIT_REPO_NAME}.git HEAD:main
                '''
            }
        }
    }
  }
}
